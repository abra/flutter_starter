name: example_feature
description: "Example feature — demonstrates the BLoC + repository pattern."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  equatable: ^2.0.5
  component_library:
    path: ../../component_library

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^10.0.0
